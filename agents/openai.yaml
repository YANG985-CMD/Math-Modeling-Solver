interface:
  display_name: "Math Modeling Playbook"
  short_description: "Evidence-gated modeling, reproducibility, and paper delivery."
  default_prompt: "Use $math-modeling-playbook to solve this problem with a baseline, executable evidence, robustness checks, reproducibility, and an audited paper."
